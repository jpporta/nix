# /home/jpporta/.config/tmuxinator/move.yml

name: move
root: ~/work/move78

# Project hooks
on_project_start: git fetch origin

windows:
  - editor:
      layout: main-vertical
      panes:
        - nvim .
        - ""
